import {NextResponse} from 'next/server'; import {z} from 'zod'; import {db} from '@/lib/db'; import {getCurrentUser} from '@/lib/auth'; import {getProvider} from '@/lib/providers';
const schema=z.object({name:z.string().min(2).max(80),repository:z.string().regex(/^[A-Za-z0-9_.-]+\/[A-Za-z0-9_.-]+$/),branch:z.string().min(1).max(100),buildCommand:z.string().max(500),startCommand:z.string().max(500),rootDirectory:z.string().max(200).optional(),planId:z.string().min(1)});
export async function POST(req:Request){try{const user=await getCurrentUser();if(!user?.customer) return NextResponse.json({error:'Unauthorized'},{status:401});const b=schema.parse(await req.json());const plan=await db.plan.findUnique({where:{id:b.planId}});if(!plan||!plan.active)return NextResponse.json({error:'Plan unavailable'},{status:400});const provider=getProvider();const resource=await provider.createApplication({name:b.name,repo:`https://github.com/${b.repository}.git`,branch:b.branch,buildCommand:b.buildCommand,startCommand:b.startCommand,rootDirectory:b.rootDirectory});const app=await db.application.create({data:{customerId:user.customer.id,name:b.name,repository:b.repository,branch:b.branch,buildCommand:b.buildCommand,startCommand:b.startCommand,rootDirectory:b.rootDirectory||null,internalDomain:resource.internalUrl,provider:process.env.PROVIDER||'local',providerResourceId:resource.resourceId,status:'DEPLOYING',deploymentStatus:'QUEUED',subscription:{create:{customerId:user.customer.id,planId:plan.id,status:'TRIALING'}}}});const d=await db.deployment.create({data:{applicationId:app.id,branch:b.branch,status:'BUILDING',buildStartedAt:new Date(),commitMessage:'Initial deployment'}});try{const dep=await provider.deploy(resource.resourceId);await db.deployment.update({where:{id:d.id},data:{status:dep.status==='SUCCESS'?'SUCCESS':'QUEUED',logs:dep.logs,buildFinishedAt:new Date(),deploymentFinishedAt:new Date()}});await db.application.update({where:{id:app.id},data:{status:dep.status==='SUCCESS'?'LIVE':'FAILED',deploymentStatus:dep.status}})}catch(e){await db.deployment.update({where:{id:d.id},data:{status:'FAILED',errorMessage:e instanceof Error?e.message:'Deployment failed',buildFinishedAt:new Date()}});await db.application.update({where:{id:app.id},data:{status:'FAILED',deploymentStatus:'FAILED'}})}return NextResponse.json({id:app.id});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Invalid request'},{status:400});}}
