'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function AppActions({id,status}:{id:string,status:string}){const [busy,setBusy]=useState(false),router=useRouter();async function act(action:string){setBusy(true);await fetch(`/api/applications/${id}/${action}`,{method:'POST'});setBusy(false);router.refresh()}return <div className="actions">{status==='SUSPENDED'?<button className="btn" disabled={busy} onClick={()=>act('resume')}>Resume</button>:<button className="btn secondary" disabled={busy} onClick={()=>act('suspend')}>Suspend</button>}<button className="btn secondary" disabled={busy} onClick={()=>act('restart')}>Restart</button><button className="btn" disabled={busy} onClick={()=>act('deploy')}>Redeploy</button></div>}
