type Usage={recordedAt:string;cpuPercent:number;ramPercent:number;storagePercent:number;bandwidthPercent:number;requests:number;concurrentUsers:number};
type Plan={requests:number;concurrentUsers:number};
function pct(value:number,limit:number){return limit>0?Math.min(100,(value/limit)*100):0;}
function UsageBar({label,value,detail}:{label:string;value:number;detail:string}){return <div style={{marginBottom:14}}><div className="actions" style={{justifyContent:'space-between',marginBottom:4}}><strong>{label}</strong><span>{detail}</span></div><div className="bar"><i style={{width:`${Math.min(100,Math.max(0,value))}%`}}/></div>}
export default function UsageDashboard({usage,plan}:{usage:Usage|null;plan:Plan|null}){if(!usage)return <div className="card" style={{marginTop:18}}><h2>Usage</h2><p className="muted">No usage data has been collected yet.</p></div>;const requestPct=pct(usage.requests,plan?.requests||0),userPct=pct(usage.concurrentUsers,plan?.concurrentUsers||0);return <div className="card" style={{marginTop:18}}><div className="actions" style={{justifyContent:'space-between'}}><div><h2>Usage</h2><p className="muted">Latest measurements collected by Bridge.</p></div><span className="muted">Updated {new Date(usage.recordedAt).toLocaleString()}</span></div><UsageBar label="CPU" value={usage.cpuPercent} detail={`${usage.cpuPercent.toFixed(0)}%`} /><UsageBar label="RAM" value={usage.ramPercent} detail={`${usage.ramPercent.toFixed(0)}%`} /><UsageBar label="Storage" value={usage.storagePercent} detail={`${usage.storagePercent.toFixed(0)}%`} /><UsageBar label="Bandwidth" value={usage.bandwidthPercent} detail={`${usage.bandwidthPercent.toFixed(0)}%`} /><UsageBar label="Requests" value={requestPct} detail={plan?.requests?`${usage.requests.toLocaleString()} / ${plan.requests.toLocaleString()} (${requestPct.toFixed(0)}%)`:`${usage.requests.toLocaleString()} requests`} /><UsageBar label="Concurrent users" value={userPct} detail={plan?.concurrentUsers?`${usage.concurrentUsers} / ${plan.concurrentUsers} (${userPct.toFixed(0)}%)`:`${usage.concurrentUsers} users`} /></div>}
