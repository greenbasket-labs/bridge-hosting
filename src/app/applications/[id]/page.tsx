import {redirect,notFound} from 'next/navigation'; import {getCurrentUser} from '@/lib/auth'; import {db} from '@/lib/db'; import AppActions from './actions';
export default async function ApplicationPage({params}:{params:Promise<{id:string}>}){const user=await getCurrentUser();if(!user?.customer)redirect('/login');const {id}=await params;const app=await db.application.findFirst({where:{id,customerId:user.customer.id},include:{subscription:{include:{plan:true}},domains:true,deployments:{orderBy:{createdAt:'desc'},take:10},usageRecords:{orderBy:{recordedAt:'desc'},take:1}}});if(!app)notFound();const u=app.usageRecords[0];return <><div className="actions" style={{justifyContent:'space-between'}}><div><h1>{app.name}</h1><span className="status">{app.status}</span><p className="muted">{app.customDomain||app.internalDomain}</p></div><AppActions id={app.id} status={app.status}/></div><div className="grid"><div className="card"><h2>Resources</h2><div className="bars">{[['CPU',u?.cpuPercent||0],['RAM',u?.ramPercent||0],['Storage',u?.storagePercent||0],['Bandwidth',u?.bandwidthPercent||0]].map(([n,v])=><div key={n}><p>{n} {Number(v).toFixed(0)}%</p><div className="bar"><i style={{width:`${Math.min(100,Number(v))}%`}}/></div></div>)}</div></div><div className="card"><h2>Plan</h2><p>{app.subscription?.plan.name}</p><p className="muted">₦{Number(app.subscription?.plan.priceKobo||0)/100}/month</p><p>CPU {app.subscription?.plan.cpuCores} · RAM {app.subscription?.plan.ramMb} MB</p></div></div><div className="card" style={{marginTop:18}}><h2>Deployments</h2><table className="table"><thead><tr><th>Created</th><th>Commit</th><th>Status</th></tr></thead><tbody>{app.deployments.map(d=><tr key={d.id}><td>{d.createdAt.toISOString()}</td><td>{d.commitSha?.slice(0,8)||'initial'}</td><td>{d.status}</td></tr>)}</tbody></table></div><div className="card" style={{marginTop:18}}><h2>Domains</h2><p className="muted">Add a domain to this application from the domain controls. DNS verification and SSL state are retained independently of deployments.</p>{app.domains.map(d=><p key={d.id}>{d.hostname} — {d.status}</p>)}</div></>}
